#include "graph.h"

/**
  Generates a random graph.

  Vertices are assigned ids from 0 to given param.
  Edges are non-deterministic.

  @param `vertices` How many vertices to generate
  @param `edges` How many edges should be generated
  @returns A graph object
*/
Graph generate_random_graph(int vertice, int edges);
